# TinyLink - example environment variables
# Copy this file to `.env` and fill values before running locally.

# Port the server listens on (optional, default 3000 if not set)
PORT=5000

# MongoDB connection string (required)
# Example (Atlas):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=

# Public base URL used to build returned short urls
# For local usage: http://localhost:5000
BASE_URL=http://localhost:5000

# Optional: set to true to use an in-memory fallback (if implemented)
# USE_IN_MEMORY_DB=false
